-- lua/user/keymaps.lua
-- User additional keymaps

local M = {}

function M.apply()
  local set = vim.keymap.set

  -- Add any user-specific keymaps here
  -- Example:
  -- set("n", "<leader>w", "<Cmd>w<CR>", { desc = "Save" })
end

return M